export type TableHead = {
  id: number;
  name: string;
};

export type EventType = {
  eventName: string;
  startDate: string;
  endDate: string;
  time: string;
  venue: string;
  iscompleted: string;
};
